# Backend

Server-side responsibilities: account/session authentication, encrypted device pairing metadata, websocket relay, audit events, and optional model orchestration. Do not store raw API keys in source control.
