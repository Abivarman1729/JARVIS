# Orchestration service

Accept normalized intents from the client, validate capability grants, route allowed tool calls, and return structured results. Keep model credentials server-side when possible.
